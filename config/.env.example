# Puz-Feed Configuration
# Copy this file to .env and customize as needed

# Path where database and other config files are stored
# Default: ./config (same directory as this file)
# CONFIG_PATH=/path/to/config

# Database configuration
# The database file will be created at: ${CONFIG_PATH}/puz-feed.db

# Session secret key for encrypting session cookies
# IMPORTANT: Generate a secure random key and keep it secret!
# You can generate one using: python -c "import secrets; print(secrets.token_hex(32))"
SESSION_SECRET_KEY=change-me-to-a-random-secret-key

# API keys (for future use)
# CLOUDMAILIN_SECRET=your-cloudmailin-secret
